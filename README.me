Starter skeleton using Vite, React 18, Tailwind CSS 3, Prettier and ESLint.

use `npx eslint "./**" --fix` to eslint the entire codebase.
use `npx prettier --write . ` to prettify the entire codebase.
